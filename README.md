# Morse_code

# 🧩 Morse Code Encoder & Decoder  

A simple, interactive **Morse Code Web App** built using **HTML, CSS, and JavaScript**.  
It allows you to **convert text to Morse code**, **decode Morse code to text**, and even **play the sound** of the Morse code beeps.

---

## 🚀 Features

- 🔤 Encode **Text → Morse Code**  
- 🔁 Decode **Morse Code → Text**  
- 🔈 **Play sound** for Morse dots and dashes  
- 🧩 Supports **A–Z, 0–9, and symbols** like `. , ? ! @ # / ( ) - + =` etc.  
- 📱 Fully responsive and modern UI  
- ⚙️ Works offline — no dependencies, no internet needed  

---

## 🧠 How It Works

### ➡️ Encode (Text → Morse)
1. Enter your message in the **Text box**.  
2. Click **“Encode → Morse”** to get the Morse code output.  
3. Click **“Encode & Play”** to hear the Morse signal as beeps.

### ⬅️ Decode (Morse → Text)
1. Enter Morse code using `.` for dots, `-` for dashes.  
2. Separate letters with a **space**, and words with a **/**.  
3. Click **“Decode → Text”** to convert it back into readable text.  

---

## ⚙️ Tech Stack

| Technology | Purpose |
|-------------|----------|
| **HTML5** | App structure |
| **CSS3** | Styling and layout (glassmorphism style) |
| **JavaScript (ES6)** | Logic for encoding, decoding, and audio beep generation |

---

## 🖥️ Live Usage

Simply open the `index.html` file in any browser —  
no setup, no server, no installation needed.  

```bash
# Clone the repository
git clone https://github.com/yourusername/morse-code-encoder-decoder.git

# Open in browser
cd morse-code-encoder-decoder
start index.html
